---
alwaysApply: true
description: Database migration conventions and Laravel 12 caveats
---

# Migration Rules

-   When modifying columns, specify all previous attributes to avoid losing defaults, nullability, or indexes.
-   Prefer Eloquent relationships and cascading rules in migrations via foreign keys.
-   Use proper indexing for frequently queried columns.
-   Keep migrations idempotent and ordered; avoid editing committed migrations—create a new one to change schema.
